import { describe, expect, it } from "vitest";import { canAccessApp, isApprovedDomain, isBootstrapAdmin, normalizeEmail } from "../src/lib/authz";
describe('authorization helpers',()=>{it('normalizes email',()=>expect(normalizeEmail('  WHuggins@LAW.Stanford.edu ')).toBe('whuggins@law.stanford.edu'));it('allows only approved domains',()=>{expect(isApprovedDomain('a@stanford.edu')).toBe(true);expect(isApprovedDomain('a@law.stanford.edu')).toBe(true);expect(isApprovedDomain('a@example.edu')).toBe(false)});it('recognizes bootstrap administrators',()=>expect(isBootstrapAdmin('WHUGGINS@law.stanford.edu')).toBe(true));it('requires domain plus approval or invitation except bootstrap admins',()=>{expect(canAccessApp({email:'person@stanford.edu',state:'pending_approval'})).toBe(false);expect(canAccessApp({email:'person@stanford.edu',invitationValid:true})).toBe(true);expect(canAccessApp({email:'whuggins@law.stanford.edu'})).toBe(true);expect(canAccessApp({email:'person@example.edu',state:'active'})).toBe(false)})});
