import Link from "next/link";import { AppShell } from "@/components/AppShell";const sections=['users','invitations','roles','organization','locations','hours','positions','tasks','qualifications','leave-types','compliance','integrations','audit'];export default function Admin(){return <AppShell><h1>Admin portal</h1><p className="muted">Protected shell for super administrators and scoped managers.</p><div className="grid">{sections.map(s=><Link className="card" key={s} href={`/admin/${s}`}><h2>{s}</h2><p>Manage {s.replace('-',' ')}.</p></Link>)}</div></AppShell>}
