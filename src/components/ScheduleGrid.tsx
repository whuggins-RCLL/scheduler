import { mockShifts } from "@/lib/mock-data";
const days=["Mon","Tue","Wed","Thu","Fri"];
export function ScheduleGrid(){return <section className="card schedule" aria-labelledby="schedule-heading"><h2 id="schedule-heading">Representative weekly scheduling grid</h2><p className="muted">Prototype uses seeded fictional shifts and provides a table alternative to any future drag grid.</p><table><caption>Week schedule by day and shift details</caption><thead><tr>{days.map(d=><th key={d} scope="col">{d}</th>)}</tr></thead><tbody><tr>{days.map(day=><td key={day}>{mockShifts.filter(s=>s.day===day).map(s=><article className="shift" key={`${s.employee}-${s.start}`} aria-label={`${s.employee} ${s.position} ${s.start} to ${s.end}`}><strong>{s.start}–{s.end}</strong><br/>{s.employee}<br/><span>{s.position}</span><br/><small className="muted">{s.location} · {s.status} · {s.tasks.join(', ')}</small></article>)}{!mockShifts.some(s=>s.day===day)&&<span className="muted">No seeded shifts</span>}</td>)}</tr></tbody></table></section>}
